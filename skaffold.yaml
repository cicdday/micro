apiVersion: skaffold/v1beta2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/cloudpocstation/micro:{{.VERSION}}"
  artifacts:
  - image: cloudpocstation/micro
    context: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/cloudpocstation/micro:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: micro
        chartPath: charts/micro
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/cloudpocstation/micro"
          image.tag: "{{.TAG}}"
